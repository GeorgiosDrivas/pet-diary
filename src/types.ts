export interface AppointmentsType {
  title: string;
  doctor: string;
  date: string;
  notes?: string;
}

export interface MedicationType {
  medicationName: string;
  date: string;
  notes?: string;
}
